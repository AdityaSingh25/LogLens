name: CI — query-service

on:
  push:
    paths: ['services/query-service/**', 'contracts/**']
  pull_request:
    paths: ['services/query-service/**', 'contracts/**']

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: '21', distribution: 'temurin', cache: 'maven' }
      - run: mvn -pl services/query-service package -DskipTests -q
      - run: mvn -pl services/query-service test -q
